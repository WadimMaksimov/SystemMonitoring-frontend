import { Logo } from '../../../shared/ui/logo/Logo';
import * as Styled from './styled';

export const Header = () => {
  return (
    <Styled.HeaderNavWrapper>
      <Logo />
    </Styled.HeaderNavWrapper>
  );
};
